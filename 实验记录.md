# CS336 Assignment 5: Alignment 实验记录

> 课程：Stanford CS336 Spring 2025
> 作业：Assignment 5 - Alignment
> 模型：Qwen 2.5 Math 1.5B
> 数据集：GSM8K（baseline/SFT/GRPO）+ 本地 MATH-format（EI）
> 当前训练环境：NSCC（新加坡）— 4x NVIDIA A100 40GB per job
> 历史迁移环境：AutoDL（国内）— 2x NVIDIA A800 80GB PCIe

---

## Day 1 (2026-02-09) — 环境配置

### 完成的工作

1. **CUDA 12.4 配置**
   - 安装位置：`/scratch/users/nus/e1553316/cuda-12.4`
   - 配置 `~/.bashrc`：设置 `CUDA_HOME`、`PATH`、`LD_LIBRARY_PATH`

2. **Python 依赖安装**
   - `uv sync --no-install-package flash-attn` 安装基础依赖
   - flash-attn 需要 GPU 编译，登录节点无法完成

3. **flash-attn 编译**（在 GPU 计算节点完成）
   - 申请交互式 GPU 节点：`qsub -I -l select=1:ngpus=1 -l walltime=01:00:00`
   - 设置 `TORCH_CUDA_ARCH_LIST="8.0"` (A100)、`MAX_JOBS=4`
   - `uv sync` 编译成功，耗时约 4 分钟
   - flash-attn==2.7.4.post1 安装完成，编译结果持久化在 `.venv/` 中

4. **模型下载**
   - 从 HuggingFace 下载 `Qwen/Qwen2.5-Math-1.5B`
   - 存放位置：`/scratch/users/nus/e1553316/assignment5/models/Qwen2.5-Math-1.5B`
   - 模型文件完整：config.json, model.safetensors, tokenizer.json 等

5. **路径配置**
   - 原作业路径 `/data/a5-alignment/models/` 在 NSCC 上无写权限
   - 修改 `tests/conftest.py` model_id 指向实际模型路径
   - 更新 CLAUDE.md 保持一致

6. **创建项目文档**
   - CLAUDE.md — Claude Code 项目上下文
   - 学习记录.md — 学习笔记
   - 实验记录.md — 本文件

### 当前状态

- [x] CUDA 12.4 配置完成
- [x] Python 依赖安装完成
- [x] flash-attn 编译完成
- [x] 模型下载完成
- [x] 路径配置完成
- [ ] adapters.py 函数实现（0/11 必做 + 0/5 可选）
- [ ] 单元测试通过
- [ ] 训练实验

### 待办

- 开始实现 `tests/adapters.py` 中的函数（Phase 1: SFT, Phase 2: GRPO）
- GSM8K 数据集会在首次使用时自动下载

---

## Day 2 (2026-02-14) — 首次完整评估（Qwen2.5-Math-1.5B on GSM8K test）

### 执行过程

1. **申请单卡交互节点**
   - 命令：`qsub -I -l select=1:ngpus=1:mem=64gb -l walltime=04:00:00`
   - 说明：1 卡 A100 40GB 对 1.5B 模型评估已足够

2. **评估命令修正**
   - 错误写法：`uv python cs336_alignment/math_baseline.py`
   - 正确写法：`uv run python -m cs336_alignment.math_baseline`

3. **数据格式适配**
   - `math_baseline.py` 依赖 `data/gsm8k/test.jsonl`
   - 已补齐并确认可读：`data/gsm8k/test.jsonl`（1319 条）

4. **运行中问题与处理**
   - `SyntaxWarning`（`drgrpo_grader.py` 中 regex 转义）为 warning，不阻塞评估
   - vLLM 在 PBS 环境下遇到 `CUDA_VISIBLE_DEVICES=GPU-UUID` 兼容问题，已在 `math_baseline.py` 增加 UUID -> index 兼容处理

### 评估结果（test split）

- 样本数：`1319`
- Accuracy（reward=1 比例）：`0.0348749052`（约 `3.49%`）
- Format reward：`0.2934040940`（约 `29.34%`）
- Type 统计：
  - Type1（格式正确 + 答案正确）：`46`
  - Type2（格式正确 + 答案错误）：`341`
  - Type3（格式错误）：`932`

### 输出文件

- 结果日志：`/scratch/users/nus/e1553316/assignment5/models/Qwen2.5-Math-1.5B/test_log.json`

### 当前状态更新

- [x] GSM8K test 基线评估已跑通（单卡 vLLM）
- [ ] adapters.py 函数实现（0/11 必做 + 0/5 可选）
- [ ] 单元测试通过
- [ ] 训练实验（SFT/GRPO）

---

## Day 3 (2026-02-16) — SFT 训练跑通 + 周期评估 + W&B 记录

### 完成的工作

1. **SFT 核心训练流程跑通**
   - 在 `cs336_alignment/sft.py` 中完成 micro-batch 训练主循环
   - 启用梯度累积与梯度裁剪：`clip_grad_norm_(..., 1.0)`
   - 训练期间 loss 与 grad_norm 均可持续记录

2. **周期性评估流程跑通（vLLM）**
   - 训练中每 `1000` 个 step 触发一次评估
   - 将当前 checkpoint 保存到 `sft_eval/{step}/`
   - 对 `GSM8K test`（`1319` 条）执行评估并写入 `test_log.json`

3. **完整一次 SFT 实验完成**
   - 总 step：`22410`
   - 评估点：`0, 1000, 2000, ..., 22000`（共 23 个）
   - 已得到完整 accuracy 曲线与 type1/type2/type3 变化曲线

4. **W&B 记录与依赖版本修正**
   - 受集群网络代理影响，采用 `offline` 记录
   - `wandb` 版本固定为 `0.24.2`（`pyproject.toml` + `uv sync`）
   - 离线目录已保存，可后续同步或导出

### 关键结果（本次 full run）

- **最终 accuracy**（step=22000）：`0.3282789992`（约 `32.83%`）
- **最佳 accuracy**（step=18000）：`0.3305534496`（约 `33.06%`）
- **最终类型统计**（step=22000）：
  - Type1（格式正确 + 答案正确）：`433`
  - Type2（格式正确 + 答案错误）：`606`
  - Type3（格式错误）：`280`
- **最终训练指标**（W&B summary）：
  - `train/loss = 1.9609375`
  - `train/grad_norm = 138`
  - 运行时长约 `2776.93s`（约 `46.3` 分钟）

### 结果解读

- accuracy 从基线（约 `3.49%`）显著提升到 `32%+`，已超过题目要求的 `15%`。
- 后期进入平台区间（约 `31%~33%` 波动），主要瓶颈从“格式错误”转为“格式正确但答案错误”（Type2 偏高）。

### 输出文件

- 周期评估日志：
  - `sft_eval/*/test_log.json`
  - `cs336_alignment/sft_eval/*/test_log.json`
- W&B 离线 run：
  - `wandb/offline-run-20260216_001131-259v26t8`
  - `wandb/offline-run-20260216_001131-259v26t8/files/wandb-summary.json`
- 曲线截图：
  - `assert/sft_experiment/accuracy.png`
  - `assert/sft_experiment/loss.png`
  - `assert/sft_experiment/grad_norm.png`
  - `assert/sft_experiment/type1.png`
  - `assert/sft_experiment/type2.png`
  - `assert/sft_experiment/type3.png`

### 当前状态更新

- [x] GSM8K baseline 评估
- [x] SFT 训练流程跑通（含周期评估）
- [x] 达到 >15% 验证准确率目标
- [ ] 多数据规模对比实验（128/256/512/1024/full）
- [ ] 过滤正确样本后的第二组 SFT 对比实验

---

## Day 4 (2026-02-20) — AutoDL 环境迁移 + 路径适配

### 环境与依赖

1. **实例环境确认**
   - 平台：AutoDL（国内）
   - 镜像：PyTorch 25.1 + CUDA 12.4
   - 硬件：2x A800 80GB, CPU 28 cores, RAM 200GB

2. **依赖安装策略（国内网络）**
   - `uv` 通过国内镜像安装（`python3 -m pip install -U uv -i https://pypi.tuna.tsinghua.edu.cn/simple`）
   - 项目依赖使用 `uv sync --no-install-package flash-attn` + `uv sync`

3. **flash-attn 编译**
   - `TORCH_CUDA_ARCH_LIST=8.0`
   - `MAX_JOBS=8`
   - 结果：`flash-attn==2.7.4.post1` 编译并导入成功

4. **模型与缓存路径**
   - 模型路径：`/root/autodl-tmp/models/Qwen2.5-Math-1.5B`
   - 建议缓存路径：
     - `HF_HOME=/root/autodl-tmp/cache/huggingface`
     - `TORCH_HOME=/root/autodl-tmp/cache/torch`
     - `VLLM_CACHE_ROOT=/root/autodl-tmp/cache/vllm`

### 代码与评估适配

1. **路径兼容修改**
   - `tests/conftest.py`：`model_id` 改为环境变量优先（`ASSIGNMENT5_MODEL_ID` / `MODEL_ID`）
   - `cs336_alignment/math_baseline.py`：`__main__` 改为自动探测 AutoDL 路径
   - `cs336_alignment/sft.py`：`model_path` 改为自动探测

2. **AutoDL 基线评估（GSM8K test）**
   - 命令：`uv run python -m cs336_alignment.math_baseline`
   - 结果（n=1319）：
     - accuracy: `0.034875`
     - format_reward: `0.293404`
     - type1/type2/type3: `46 / 341 / 932`

---

## Day 5 (2026-02-21) — Expert Iteration (MATH-format) 实验

### 实验脚本与数据

- 脚本：`cs336_alignment/expert_iteration_experiment.py`
- 训练集：`data/math/train.jsonl`（加载后 11998 条）
- 验证集：`data/math/test.jsonl`

### 先做 smoke test（流程验证）

- 参数：
  - `n_ei_steps=1`
  - `db_size=64`
  - `rollouts_per_question=2`
  - `sft_epochs_per_ei=1`
  - `max_eval_samples=64`
- 结果：
  - step0 accuracy: `0.0312`
  - step1 accuracy: `0.0469`
  - 结论：EI 训练闭环可运行（rollout -> 选优 -> SFT -> eval）

### 正式 EI 运行（单组）

1. **运行参数（完整记录）**
   - `n_ei_steps=5`
   - `db_size=512`
   - `rollouts_per_question=4`
   - `sft_epochs_per_ei=1`
   - `micro_batch_size=1`
   - `local_batch_size=32`
   - `lr=1e-5`
   - `max_eval_samples=500`
   - `rollout_temperature=1.0`
   - `rollout_top_p=1.0`
   - `rollout_max_tokens=1024`
   - `eval_temperature=1.0`
   - `eval_top_p=1.0`
   - `eval_max_tokens=1024`
   - `only_positive_rollouts=false`
   - `vllm_device=cuda:1`
   - `gpu_memory_utilization=0.8`
   - `output_dir=/root/autodl-tmp/assignment5_outputs/cs336_alignment/ei_runs_formal_db512_g4_e1`

2. **step 指标（metrics_history.json）**
   - step0: `acc=0.040`, `format=0.244`
   - step1: `acc=0.056`, `format=0.392`
   - step2: `acc=0.070`, `format=0.462`
   - step3: `acc=0.064`, `format=0.538`
   - step4: `acc=0.076`, `format=0.570`
   - step5: `acc=0.110`, `format=0.606`

3. **趋势解读**
   - accuracy 从 `4.0%` 提升到 `11.0%`（+7.0%）
   - format reward 提升明显（`0.244 -> 0.606`），模型先学会格式，再逐步提升答案正确率
   - `train/response_entropy` 在 `~0.82-0.89` 区间波动，未出现明显熵塌缩
   - 本组仍低于 `15%` 目标，后续需继续调参（如提升 `G`、`db_size`、`sft_epochs_per_ei`）

### W&B 与存储管理

1. **W&B 同步**
   - 离线 run 已同步：
     - `5ocq572h`
     - `thrmsfms`
     - `6zze4mni`（正式 EI）

2. **输出迁移到数据盘**
   - 已将大体积目录迁移到 `/root/autodl-tmp/assignment5_outputs/cs336_alignment/`
   - 项目内保留软链接：
     - `cs336_alignment/ei_runs_smoke`
     - `cs336_alignment/ei_runs_db512_g4_e1`
   - 系统盘使用率从 `93%` 降至 `35%`

### 当前状态更新

- [x] AutoDL 环境配置与路径适配完成
- [x] EI 脚本在 MATH-format 数据集上跑通
- [x] EI 单组正式实验完成（`db=512, G=4, sft_epoch=1, n_ei_steps=5`）
- [ ] EI 达到 >=15%（待后续调参）

---

## Day 6 (2026-02-24) — GRPO Adapter 对接与定向单测

### 完成的工作

1. **Adapter 对接**
   - 在 `tests/adapters.py` 中完成：
     - `run_compute_naive_policy_gradient_loss` ->
       `cs336_alignment.utils.compute_naive_policy_gradient_loss`
   - `run_compute_group_normalized_rewards` 保持已对接状态并复测

2. **定向 pytest 验证**
   - 执行命令：
     - `uv run pytest tests/test_grpo.py -k 'compute_group_normalized_rewards or compute_naive_policy_gradient_loss' -q`

### 测试结果

- `test_compute_group_normalized_rewards_normalize_by_std`: PASSED
- `test_compute_group_normalized_rewards_no_normalize_by_std`: PASSED
- `test_compute_naive_policy_gradient_loss`: PASSED
- 汇总：`3 passed, 11 deselected, 1 warning in 2.27s`
- warning 说明：
  - `transformers` 提示 `TRANSFORMERS_CACHE` 将弃用（FutureWarning），不影响本次测试通过

### 当前状态更新

- [x] `run_compute_group_normalized_rewards` adapter 已对接并通过测试
- [x] `run_compute_naive_policy_gradient_loss` adapter 已对接并通过测试
- [ ] `run_compute_grpo_clip_loss`
- [ ] `run_compute_policy_gradient_loss`
- [ ] `run_masked_mean`
- [ ] `run_grpo_microbatch_train_step`

---

## Day 7 (2026-02-27) — GRPO Train Loop 分阶段实现推进

### 今日完成

1. **GRPO train loop 主体骨架推进（A~G）**
   - 文件：`cs336_alignment/grpo_train_loop.py`
   - 已落地阶段：
     - Phase A：参数检查与中间量推导（`micro_train_batch_size` 等）
     - Phase B：按 prompt 采样并扩展到 group rollout
     - Phase C：vLLM rollout + 输出格式规范化
     - Phase D：`compute_group_normalized_rewards` 计算奖励/优势并做 finite 检查
     - Phase E：tokenize + policy log-prob 前向
     - Phase F：`grpo_clip` 下 `old_log_probs` 缓存（detach）
     - Phase G：microbatch 训练、梯度累积、`clip_grad_norm_`、尾部 step 保护

2. **数据集适配整理**
   - 当前 GRPO/EI 主数据源统一为 `data/math/*.jsonl`
   - 新增并使用 `cs336_alignment/data_utils.py::extract_question_and_gt`，
     同时兼容：
     - MATH 字段：`problem` / `answer`
     - GSM8K 回退：`question` / `answer`（支持 `####` 解析）

3. **可读性改进**
   - 在 `grpo_train_loop.py` 增加并中文化了 `step / batch / microbatch` 注释对照，便于后续调参与排错。

### 当前未完成（留到明天）

- `epochs_per_rollout_batch` 还未真正生效（当前仍是每个 rollout 一轮更新）。
- Phase H（验证与日志）尚未接入：`val/*` 指标、周期评估、样例输出记录。
- CLI 入口结构（`Args + main`）尚未整理完成。

### 明日优先级

1. 先接通 `epochs_per_rollout_batch`（为 off-policy 任务做准备）。
2. 再补 Phase H 的验证与日志闭环（可画曲线并支持后续实验对比）。

---

## Day 8 (2026-03-06) — 返回新加坡 + 切回 NSCC 服务器

### 完成的工作

1. **当前工作环境切回 NSCC**
   - 人已回到新加坡，当前默认开发与训练环境回到 NSCC
   - 当前登录节点：`asp2a-login-nus02`
   - 当前项目路径：`/scratch/users/nus/e1553316/assignment5`

2. **默认模型路径与环境说明切回 NSCC**
   - 默认模型路径恢复为：
     `/scratch/users/nus/e1553316/assignment5/models/Qwen2.5-Math-1.5B`
   - 后续命令、路径和资源说明默认以 NSCC 为准

3. **文档同步**
   - 已更新 `CLAUDE.md`
   - 已更新本实验记录顶部环境摘要
   - AutoDL 保留为历史迁移环境记录，不再作为当前默认环境

4. **MATH 数据集重新划分**
   - 原始数据：`data/math_origin/`（train 12000 + test 500 = 12500 条）
   - 重新划分后写入 `data/math/`：
     - `data/math/train.jsonl`：7500 条（训练集）
     - `data/math/validation.jsonl`：5000 条（验证集）
   - 训练中周期验证：取验证集前 1024 条（`max_eval_samples=1024`）
   - 最终测试：使用全部 5000 条（`max_eval_samples=0`）

5. **GRPO train loop Phase H 完成（验证与日志闭环）**
   - 文件：`cs336_alignment/grpo_train_loop.py`
   - 新增 `_evaluate_on_validation()` 函数：
     - 固定取验证集前 N 条（不随机），确保 step 间结果可比
     - 聚合 `val/reward`、`val/format_reward`、`val/answer_reward`
     - 统计 type1/type2/type3 分布
   - 新增 `_build_sampling_params()` 辅助函数：
     - 训练 rollout 和 eval rollout 各有独立的 `sampling_params`
     - 可分别设置 `temperature`、`top_p`、`max_tokens`
   - Step 0 基线评估：训练开始前先跑一次 validation，作为 baseline
   - 周期验证逻辑：
     - 按 `eval_every_steps` 周期评估
     - 最后一个 step 无论如何都补一次评估
   - `epochs_per_rollout_batch` 确认已生效：
     - `grpo_clip` 模式：epoch 0 复用缓存的 `old_log_probs`，后续 epoch 重新前向
     - 其他 loss 模式：每 epoch 都重新做 forward pass

### GRPO 代码审查修复（3 个 Finding）

使用 Codex 对 `grpo_train_loop.py` + `utils.py` 做了代码审查，发现 3 个问题。

**Finding 1（高严重度）— 多 microbatch backward 崩溃**
- 问题：全量 forward 一次后在 microbatch 循环内对 slice 反复 `backward()`，第二个 microbatch 崩溃。
- 修复：forward pass 移入 microbatch 循环内部，各自独立计算图；Phase F 的 `old_log_probs` 改用 `torch.no_grad()` 包裹。

**Finding 2（中高严重度）— Deliverable 未闭环**
- 问题：`metrics_history` 仅在内存中，无落盘、无 CLI 入口。
- 修复：新增 `output_dir` 参数增量覆写 `metrics_history.json` + `rollout_examples.json`；新增 `parse_args()` + `main()` CLI 入口；`main()` 落盘 `config.json`、`final_checkpoint/`。

**Finding 3（中严重度）— clip_fraction 统计口径不对**
- 问题：`clip_fraction` 在全部 token（含 prompt/padding）上算 mean，导致指标被稀释。
- 修复：改用 `clipped_mask * response_mask` 限定在 response token 上计算。

### 当前状态更新

- [x] 当前默认运行环境切回 NSCC，MATH 数据集重新划分（7500 train + 5000 validation）
- [x] GRPO train loop Phase H 验证与日志闭环完成
- [x] GRPO 代码审查 3 个 Finding 全部修复
- [x] GRPO smoke test 通过（2 steps，reinforce_with_baseline，lr=1e-5）
- [ ] GRPO 正式实验运行

---

## Day 9 (2026-03-07) — GRPO 完善 + Baseline/SFT/Filtered 实验全部完成

### GRPO 代码改进

1. **tqdm 进度条**：外层 GRPO step 循环加上 `tqdm(range(n_grpo_steps), desc="GRPO steps")`，内层 microbatch 循环也有嵌套进度条（`leave=False`）。

2. **UUID 归一化重构**：`grpo_train_loop.py` 中删除重复的 `_normalize_cuda_visible_devices` 实现，改为直接 import `math_baseline._normalize_cuda_visible_devices_for_vllm`。

3. **W&B 支持（offline 模式）**：
   - 新增 CLI 参数：`--wandb-project`、`--wandb-run-name`、`--wandb-offline`（默认 True）
   - 不传 `--wandb-project` 则完全不启用
   - 上传命令：`wandb sync grpo_runs/<run_name>/wandb/`

4. **正式实验配置**（`run_grpo.sh`）：
   - `--output-dir outputs/grpo/run1_grpo_clip`
   - `--n-grpo-steps 256 --eval-every-steps 10`
   - `--loss-type grpo_clip --lr 1e-6`
   - 其余超参用默认值（rollout_batch_size=256, group_size=8, gradient_accumulation_steps=128）

---

### Baseline 正式评估结果（MATH validation）

- **脚本**: `run_math_baseline.sh`，模型：Qwen2.5-Math-1.5B 原始权重
- **数据集**: `data/math/validation.jsonl`，1024 条（`seeded_random`, `seed=42`）
- **输出目录**: `outputs/baseline/math_val_1024_seed42/`

| 指标 | 数值 |
|------|------|
| accuracy | **4.10%** |
| type1（格式✓ 答案✓） | 42 |
| type2（格式✓ 答案✗） | 241 |
| type3（格式✗） | 741 |

格式合规率（type1+type2）= 27.6%，大多数输出不满足格式要求（type3 占 72.4%）。该结果作为后续实验对比基准。

---

### SFT 多规模实验结果

- **脚本**: `run_sft_all.pbs`（PBS 批处理，顺序执行）
- **超参**: `epochs=3, lr=1e-5, micro_batch_size=1, local_batch_size=32`
- **评估方式**: step 0 baseline eval + final eval（无周期性 eval）

| N（训练样本数） | step 0 accuracy | final accuracy | 耗时 |
|---|---|---|---|
| 128 | 4.10% | **11.0%** | ~（交互节点，单独跑） |
| 256 | 3.81% | **12.5%** | ~10 min |
| 512 | 3.81% | **15.2%** | ~4 min |
| 1024 | 3.81% | **16.4%** | ~6 min |
| full (7500) | 3.81% | **18.9%** | ~34 min |

accuracy 随训练样本数单调递增（清晰的 data scaling 趋势）。N=512 时已超过作业要求的 15% 目标线。

---

### Filtered SFT 实验结果（Rejection Sampling Fine-tuning）

**方法**：
1. 对 7500 题各用 vLLM 生成 K=4 条 rollout（共 30000 条）
2. 用 `r1_zero_reward_fn` 打分，只保留 `answer_reward=1` 的条目
3. 存为 `data/math/train_filtered.jsonl`，再用 `sft.py` 训练（相同超参）

- **脚本**: `run_filtered.pbs`（生成+训练两步一体）
- **过滤后数据量**: 约 **1197 条**（推算：112 opt_steps × 32 batch / 3 epochs）

| 指标 | 数值 |
|------|------|
| step 0 accuracy | ~3.8% |
| **final accuracy** | **16.0%** |
| type1 / type2 / type3 | 164 / 461 / 399 |
| train/opt_step | 112 |

**与常规 SFT 对比**：

| 方法 | 样本数 | final accuracy |
|------|--------|----------------|
| SFT N=1024（人工标注） | 1024 | 16.4% |
| **SFT filtered（模型生成过滤）** | **~1200** | **16.0%** |
| SFT full（全量人工标注） | 7500 | 18.9% |

~1200 条模型自生成的正确解，效果与 1024 条人工标注几乎相同，验证了 RFT 的有效性。

---

### 当前状态

- [x] GRPO smoke test 通过，tqdm / wandb / UUID 归一化完善
- [x] Baseline 正式评估完成（MATH validation, 4.10%）
- [x] SFT 多规模实验（N=128/256/512/1024/full）全部完成
- [x] Filtered SFT 实验完成（RFT, 16.0%）
- [ ] GRPO 正式实验运行

---

## Day 10 (2026-03-08) — EI 控制变量实验全部完成（NSCC）

### 实验设计

在 NSCC 上通过 `submit_ei_all.sh` 一键提交 6 个 PBS job，对 Expert Iteration 的三个超参做独立控制变量实验：

| 变量 | 取值范围 | baseline 值 |
|------|----------|-------------|
| G（rollouts_per_question） | 4, **8** | 4 |
| D_b（db_size） | **512**, 1024, 2048 | 1024 |
| E（sft_epochs_per_ei） | **1**, 2 | 1 |

其他超参固定：`n_ei_steps=5, lr=1e-5, micro_batch_size=1, local_batch_size=32, eval_samples=1024`

并发说明：NSCC `gdev` 队列每用户最多 4 个并发 node，6 个 job 分两批（4R + 2Q）自动调度完成。

---

### 实验结果（step 0 → step 5 accuracy）

| Run | step0 | step1 | step2 | step3 | step4 | step5 |
|-----|-------|-------|-------|-------|-------|-------|
| G4_Db512_E1 | 4.10% | 6.93% | 8.11% | 8.98% | 10.6% | **13.9%** |
| G4_Db1024_E1（baseline） | 4.10% | 8.30% | 13.6% | 14.2% | 17.0% | **19.5%** |
| G4_Db2048_E1 | 4.10% | 12.4% | 14.6% | 18.5% | 18.7% | **18.8%** |
| G8_Db1024_E1 | 4.10% | 10.2% | 13.0% | 16.5% | 18.6% | **21.1%** |
| G4_Db1024_E2 | 4.10% | 11.2% | 15.2% | 18.8% | 19.2% | **18.5%** |
| G8_Db1024_E2 | 4.10% | 12.6% | 14.9% | 21.1% | 21.1% | **21.9%** |

format_reward 同步记录（step0 → step5）：

| Run | step0 | step5 |
|-----|-------|-------|
| G4_Db512_E1 | 0.265 | 0.582 |
| G4_Db1024_E1 | 0.265 | 0.680 |
| G4_Db2048_E1 | 0.265 | 0.702 |
| G8_Db1024_E1 | 0.265 | 0.729 |
| G4_Db1024_E2 | 0.265 | 0.699 |
| G8_Db1024_E2 | 0.265 | 0.745 |

---

### 结果解读

**D_b（db_size）的影响**（G=4, E=1）：
- D_b=512（13.9%）< D_b=1024（19.5%）≈ D_b=2048（18.8%）
- D_b=1024 已基本饱和，D_b=2048 在 5 步内未显示额外收益；增大 D_b 需同时延长训练步数才有意义

**G（rollouts_per_question）的影响**（D_b=1024, E=1）：
- G=4（19.5%）< G=8（21.1%）
- 更多 rollout 带来更高质量的 rejection sampling 样本，提升 EI 效果

**E（sft_epochs_per_ei）的影响**：
- G=4: E=1（19.5%）vs E=2（18.5%）——多 epoch 略有过拟合迹象，step5 比 step4 下降
- G=8: E=1（21.1%）vs E=2（21.9%）——G 大时多 epoch 有正效果
- 结论：E=2 仅在 G 较大时有意义

**最优配置**：G=8, D_b=1024, E=2，step5 accuracy=**21.9%**，显著超过 15% 目标

**与前期 AutoDL EI 对比**：AutoDL 版 D_b=512, G=4, E=1 → step5 = 11.0%；NSCC 相同配置 → 13.9%，差异来自数据集规模不同（AutoDL 用 500 条 eval，NSCC 用 1024 条 validation）

---

---

### GRPO run1（on-policy, reinforce_with_baseline, lr=1e-5）

**配置**（`outputs/grpo/run1_onpolicy/config.json`）：

| 参数 | 值 |
|------|----|
| loss_type | `reinforce_with_baseline` |
| lr | `1e-5` |
| n_grpo_steps | 256 |
| rollout_batch_size | 256 |
| group_size | 8 |
| gradient_accumulation_steps | 128 |
| epochs_per_rollout_batch | 1 |
| eval_every_steps | 10 |
| max_eval_samples | 1024 |
| sampling_max_tokens | 1024 |

注：`train_batch_size=256, gradient_accumulation_steps=128` → 每步 micro_batch_size=2（与作业文档一致）。

**周期验证结果**（eval_every_steps=10，共 27 个评估点）：

| step | val_reward | val_format_reward |
|------|------------|-------------------|
| 0 | 4.10% | 26.46% |
| 10 | 9.28% | 45.02% |
| 20 | 11.82% | 50.49% |
| 30 | 13.09% | 53.81% |
| 40 | 13.87% | 56.84% |
| 50 | 15.33% | 54.79% |
| 60 | 17.38% | 60.55% |
| 70 | 17.77% | 63.48% |
| 80 | 17.87% | 62.70% |
| 90 | 20.12% | 66.80% |
| 100 | 20.51% | 65.33% |
| 110 | 20.12% | 66.50% |
| 120 | 22.66% | 65.04% |
| 130 | 21.19% | 65.72% |
| 140 | 19.63% | 62.79% |
| 150 | 21.78% | 65.33% |
| 160 | 21.19% | 61.52% |
| 170 | 22.66% | 61.04% |
| 180 | 23.05% | 62.11% |
| 190 | 22.46% | 62.89% |
| 200 | 20.90% | 63.09% |
| 210 | 21.58% | 64.36% |
| 220 | 22.56% | 63.96% |
| 230 | 22.27% | 64.26% |
| 240 | 23.24% | 64.26% |
| 250 | 23.14% | 66.70% |
| 256（final） | **21.29%** | 64.65% |

**W&B summary**（离线 run）：

| 指标 | 值 |
|------|----|
| train/loss（final） | -0.00486 |
| train/reward_mean（final） | 0.2617 |
| train/response_entropy（final） | 0.2796 |
| val/reward（final） | 0.2129 |
| val/format_reward（final） | 0.6465 |

**关键观察**：
- val_reward 从 4.10% 稳步上升，step 100 附近突破 20%，step 180 附近达到 23%+
- step 120~256 进入震荡平台区（20~23%），未能稳定突破 25% 目标
- format_reward 快速提升至 60%+，之后与 step 数解耦，说明模型格式已收敛，制约因素转为数学能力
- entropy 从初始快速下降后趋于稳定（~0.28），未出现明显熵崩

**结论**：`reinforce_with_baseline + lr=1e-5` 在 256 步内达到峰值约 **23.2%**，final 21.3%，距离 25% 目标仍有差距。下一步进行 LR sweep（1e-6 / 3e-6 / 1e-5 / 3e-5）寻找最优学习率。

---

---

### GRPO LR Sweep（run2~run5）

超参与 run1 完全相同，仅学习率不同（`reinforce_with_baseline`，256 steps，无 KL 惩罚）。分两轮：Round 1（3e-6 / 3e-5）确定范围，Round 2（2e-5 / 1.5e-5）细化搜索。

**全量周期验证结果（5 个 run 合并对比）**：

| step | run2 3e-6 | run1 1e-5 | **run5 1.5e-5** | run4 2e-5 | run3 3e-5 |
|------|-----------|-----------|-----------------|-----------|-----------|
| 0 | 4.10% | 4.10% | 4.10% | 4.10% | 4.10% |
| 10 | 4.49% | 9.28% | 13.09% | 18.16% | 16.70% |
| 20 | 4.98% | 11.82% | 18.65% | 21.48% | 18.75% |
| 30 | 6.25% | 13.09% | 21.39% | 23.83% | 22.27% |
| 40 | 6.54% | 13.87% | 21.19% | 23.14% | 20.61% |
| 50 | 5.66% | 15.33% | 24.41% | 21.58% | 25.20% |
| 60 | 6.93% | 17.38% | 25.98% | 25.68% | 27.05% |
| 70 | 6.84% | 17.77% | 25.39% | 23.05% | 29.98% |
| 80 | 6.45% | 17.87% | 28.32% | 24.80% | 32.91% |
| 90 | 8.69% | 20.12% | 26.17% | 25.10% | 34.18% |
| 100 | 7.42% | 20.51% | 28.32% | 24.41% | **36.04%** ⚠️ |
| 110 | 9.08% | 20.12% | 25.49% | 24.12% | 32.42% |
| 120 | 6.84% | 22.66% | 26.46% | 23.14% | 32.52% |
| 130 | 9.38% | 21.19% | 27.83% | 26.27% | 34.47% |
| 140 | 8.30% | 19.63% | 26.86% | 24.02% | 31.35% ⚠️崩溃 |
| 150 | 9.28% | 21.78% | 29.30% | 23.44% | 12.79% |
| 160 | 9.86% | 21.19% | **29.39%** ⬆️ | 22.85% | 13.09% |
| 170 | 10.06% | 22.66% | 28.42% | 25.20% | 14.36% |
| 180 | 10.45% | 23.05% | 26.46% | 24.61% | 12.89% |
| 190 | 11.43% | 22.46% | 28.42% | 24.90% | 19.14% |
| 200 | 12.50% | 20.90% | 27.64% | 25.49% | 20.02% |
| 210 | 12.60% | 21.58% | 27.25% | 25.59% | 18.95% |
| 220 | 14.45% | 22.56% | 27.93% | 24.32% | 25.68% |
| 230 | 10.45% | 22.27% | 27.64% | 24.80% | 25.78% |
| 240 | 12.50% | 23.24% | 25.49% | 26.17% | 17.97% |
| 250 | 12.40% | 23.14% | 24.90% | **26.76%** ⬆️ | 21.58% |
| 256 | 12.50% | 21.29% | 24.80% | 24.61% | 24.02% |

**最终训练指标对比**：

| 指标 | run2 3e-6 | run1 1e-5 | **run5 1.5e-5** | run4 2e-5 | run3 3e-5 |
|------|-----------|-----------|-----------------|-----------|-----------|
| peak val_reward | 14.5% | 23.2% | **29.4%** | 26.8% | 36.0%⚠️ |
| final val_reward | 12.5% | 21.3% | **24.8%** | 24.6% | 24.0% |
| entropy（final） | 0.409 | 0.280 | **0.138** | 0.191 | 0.062⚠️ |
| train/loss | -0.00039 | -0.00486 | -0.01646 | -0.00148 | -0.16204 |
| train/reward_mean | 0.090 | 0.262 | **0.309** | 0.125 | 0.215 |
| 稳定性 | 欠拟合 | 稳定 | **最稳定** | 稳定 | 熵崩⚠️ |

**分析**：

- **3e-6**：严重欠拟合，256 步仅到 14.5%，format_reward 只爬到 50%
- **1e-5**：稳定但平台偏低（20~23%），收敛慢
- **1.5e-5**：step 80 起稳定维持 25~29% 高位平台，peak 29.4%，全程无崩，entropy 合理，reward_mean 最高（0.309）— **sweet spot**
- **2e-5**：收敛最快（step 10 即达 18%），但平台水位（23~27%）略低于 1.5e-5
- **3e-5**：step 100 峰值 36%，但 step 140 发生熵崩（entropy→0.062），format_reward 断崖跌至 19%，不稳定

**最终结论**：**确定使用 `lr=1.5e-5`** 作为最优配置（peak 29.4%，平台稳定，无熵崩）。

---

### GRPO Ablation：no_baseline vs reinforce_with_baseline（run6，lr=1.5e-5）

控制变量：其他超参与 run5 完全一致，仅 `loss_type` 改为 `no_baseline`（advantage = r_i，不减组内均值）。

**逐步对比（run6 no_baseline vs run5 reinforce_with_baseline）**：

| step | run6 no_baseline val | run5 with_baseline val | run6 fmt | run5 fmt |
|------|---------------------|----------------------|---------|---------|
| 0 | 4.10% | 4.10% | 26.46% | 26.46% |
| 10 | 12.40% | 13.09% | 54.88% | 52.25% |
| 20 | 13.38% | 18.65% | 67.09% | 60.94% |
| 30 | 12.89% | 21.39% | 70.02% | 69.04% |
| 40 | 16.89% | 21.19% | 75.29% | 65.43% |
| 50 | 17.19% | 24.41% | 76.66% | 67.38% |
| 60 | 17.58% | 25.98% | 80.76% | 71.00% |
| 70 | 19.14% | 25.39% | 84.86% | 69.63% |
| 80 | 17.87% | 28.32% | 85.74% | 67.77% |
| 90 | 19.53% | 26.17% | 86.23% | 67.97% |
| 100 | 21.29% | 28.32% | 88.57% | 66.80% |
| 110 | 19.63% | 25.49% | 90.72% | 67.58% |
| 120 | 21.09% | 26.46% | 90.23% | 67.97% |
| 130 | 19.63% | 27.83% | 89.75% | 66.21% |
| 140 | 21.29% | 26.86% | 90.62% | 68.46% |
| 150 | 21.97% | 29.30% | 89.84% | 70.21% |
| 160 | 20.90% | **29.39%** ⬆️ | 91.11% | 69.14% |
| 170 | 21.19% | 28.42% | 93.65% | 70.90% |
| 180 | 21.48% | 26.46% | 92.77% | 70.12% |
| 190 | 20.90% | 28.42% | 93.07% | 70.80% |
| 200 | 22.85% | 27.64% | 92.19% | 69.43% |
| 210 | 21.48% | 27.25% | 93.26% | 71.00% |
| 220 | 22.85% | 27.93% | 93.65% | 71.78% |
| 230 | 22.17% | 27.64% | 93.26% | 71.68% |
| 240 | 22.75% | 25.49% | 92.97% | 71.68% |
| 250 | **24.02%** | 24.90% | 94.24% | 68.26% |
| 256 | 23.93% | 24.80% | **95.12%** | 69.92% |

**最终指标对比**：

| 指标 | run6 no_baseline | run5 reinforce_with_baseline |
|------|-----------------|------------------------------|
| peak val_reward | 24.0%（step 250） | **29.4%**（step 160） |
| final val_reward | 23.9% | **24.8%** |
| entropy（final） | **0.927** | 0.138 |
| train/loss | +0.00096（正值） | -0.01646 |
| train/reward_mean | 0.137 | **0.309** |
| format_reward（final） | **95.1%** | 69.9% |

**分析**：

- **no_baseline 的 val_reward 明显更低**（peak 24% vs 29.4%），baseline 减去组内均值后梯度信噪比大幅提升，模型能更清晰地区分"组内谁表现更好"
- **entropy = 0.927**（几乎等于初始值），说明 no_baseline 的梯度噪声过大，policy 无法有效收敛，一直在宽泛探索
- **format_reward 高达 95.1%** 但 val_reward 只有 24%——模型学会了输出格式，但数学能力提升有限；高 format 低 accuracy 是"格式过拟合"的典型表现
- **loss 为正值（+0.00096）**：reinforce_with_baseline 的负 loss 表示 policy 在优化方向上有效收敛，no_baseline 正 loss 说明优化混乱
- `grpo_clip` 在 `epochs_per_rollout_batch=1`（on-policy）下与 `reinforce_with_baseline` 几乎等价，故不单独测试

**结论：baseline 是必要的。** 减去组内均值是 GRPO 有效训练的关键，去掉后梯度方差过大导致收敛失效。

---

### 当前状态更新

- [x] EI 6 组控制变量实验全部完成
- [x] 最优 EI 配置：G=8, D_b=1024, E=2，accuracy=21.9%
- [x] GRPO run1（lr=1e-5）完成，peak 23.2%，final 21.3%
- [x] GRPO LR sweep（run2~run5，共 4 个 lr）全部完成
- [x] **Sweet spot 确认：lr=1.5e-5，peak 29.4%，稳定无崩**
- [x] Ablation：no_baseline（run6）完成，验证 baseline 必要性
- [ ] 视情况以 lr=1.5e-5 做更长步数的 best run

---

## Day 11 (2026-03-10) — GRPO 消融实验续：masked_normalize + no_std + off-policy

### 代码变更

本日在 `cs336_alignment/utils.py` 和 `grpo_train_loop.py` 中新增两个可控参数：

| 参数 | CLI 标志 | 默认值（新） | 说明 |
|------|----------|------------|------|
| `loss_aggregation` | `--loss-aggregation` | `masked_normalize` | `masked_mean`（除以实际 token 数）vs `masked_normalize`（除以 max_gen_len）|
| `use_std_normalization` | `--use-std-normalization` | `False` | advantage 是否除以组内标准差 |

`run_grpo_sweep.pbs` 同步新增 `LOSS_AGGREGATION`、`USE_STD`、`EPOCHS`、`TRAIN_BS`、`GRAD_ACC` 五个 shell 变量，支持通过 `qsub -v` 参数化控制。

---

### GRPO Ablation：masked_normalize（run7 vs run8 vs run5）

三个 run 均使用 `lr=1.5e-5, reinforce_with_baseline, 256 steps`，仅 `loss_aggregation` 和 `use_std_normalization` 不同。

| 配置 | run5（基线） | run7 | run8 |
|------|------------|------|------|
| loss_aggregation | `masked_mean` | `masked_normalize` | `masked_normalize` |
| use_std_normalization | True | True | **False** |

**逐步对比**：

| step | run5 masked_mean | run7 normalize+std | run8 masked_normalize(no_std) |
|------|-----------------|--------------------|-----------------------|
| 0 | 4.10% | 4.10% | 4.10% |
| 10 | 13.09% | 11.43% | **16.31%** |
| 20 | 18.65% | 15.23% | **21.19%** |
| 30 | 21.39% | 20.70% | **23.54%** |
| 40 | 21.19% | 22.75% | **28.71%** |
| 50 | 24.41% | 25.10% | **28.42%** |
| 60 | 25.98% | 28.03% | **28.71%** |
| 70 | 25.39% | 28.71% | **27.64%** |
| 80 | 28.32% | **30.96%** | 29.49% |
| 90 | 26.17% | **35.06%** | 27.25% |
| 100 | 28.32% | **32.42%** | 26.27% |
| 110 | 25.49% | **30.96%** | 30.37% |
| 120 | 26.46% | **31.64%** | 31.15% |
| 130 | 27.83% | **30.96%** | 27.93% |
| 140 | 26.86% | **33.01%** | 28.52% |
| 150 | 29.30% | **33.40%** | 27.93% |
| 160 | **29.39%** ⬆️ | 31.54% | 26.37% |
| 170 | 28.42% | 30.37% | 27.25% |
| 180 | 26.46% | 31.05% | **29.00%** |
| 190 | 28.42% | 29.00% | **31.45%** ⬆️ |
| 200 | 27.64% | 30.57% | 28.91% |
| 210 | 27.25% | 31.35% | 27.73% |
| 220 | 27.93% | 28.52% | 29.20% |
| 230 | 27.64% | **31.45%** | 28.32% |
| 240 | 25.49% | 36.43% | 28.22% |
| 250 | 24.90% | **39.84%** ⬆️ | 29.00% |
| 256 | 24.80% | 36.33% | 28.42% |

**最终指标对比**：

| 指标 | run5 masked_mean | run7 normalize+std | run8 masked_normalize(no_std) |
|------|-----------------|--------------------|-----------------------|
| peak val_reward | 29.4%（step 160） | **39.8%**（step 250） | 31.5%（step 190） |
| final val_reward | 24.8% | 36.3% | 28.4% |
| format_reward（final） | 69.9% | 68.9% | **78.6%** |
| 收敛速度 | 中 | 慢（step 90 爆发） | **快**（step 10 即 16%） |

**分析**：

- **masked_normalize 显著优于 masked_mean**：run7 peak 39.8% vs run5 peak 29.4%（+10.4pp）。以 max_gen_len 为分母时，较长的正确回答贡献更多梯度，隐式鼓励模型生成更完整的思维链
- **USE_STD=true 更好**：run7（39.8%）> run8（31.5%）；std normalization 有效提升组内 advantage 对比度，让强回答和弱回答的梯度差异更大
- **run7 step 250 仍在上升**，尚未收敛，加长训练步数应有进一步提升空间
- **run8 收敛更快但天花板更低**：no_std 时 advantage 量级更大（没有除以 std），早期更新幅度大，但后期震荡，无法达到 run7 的高峰

**结论：确定 `masked_normalize + USE_STD=true` 为新默认配置**（对应 run7 参数设置）。

---

### GRPO off-policy 实验：run9A / run9B（OOM，未完成）

两个 run 均 OOM 崩溃，均未产生 `metrics_history.json`。

| 配置 | run9A | run9B |
|------|-------|-------|
| epochs_per_rollout_batch | 2 | 3 |
| train_batch_size | 256 | 128 |
| gradient_accumulation_steps | 128 | 64 |
| loss_type | grpo_clip | grpo_clip |
| 错误 | OOM | OOM |

**OOM 根因分析**：

报错为 `Tried to allocate 98.31 GiB`（GPU 仅 39.56 GiB）。原因：`grpo_clip` 需要 `old_log_probs`，代码当前对整个 rollout batch（256 条 × 1024 token）做一次完整 forward pass 以计算 `old_log_probs`，未走 microbatch 分割逻辑。logits 张量 = 256 × 1024 × 151936（vocab_size）× 2 bytes（fp16）≈ **80 GB**，远超单卡显存。

**修复方向**：将 `old_log_probs` 的计算改为 microbatch 形式（循环切片），与训练 microbatch 走同样的内存路径。

---

### 当前状态更新

- [x] masked_normalize ablation 完成（run7 / run8）
- [x] **新最优：masked_normalize + USE_STD=true（run7），peak 39.8%，仍有上升空间**
- [x] 新默认配置已更新：`loss_aggregation=masked_normalize, use_std_normalization=False`（run8 对照），run7 显式传 `USE_STD=true`
- [ ] off-policy（run9A / run9B）OOM 待修复：需将 old_log_probs 计算改为 microbatch 形式
