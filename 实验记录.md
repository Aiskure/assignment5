# CS336 Assignment 5: Alignment 实验记录

模型：Qwen 2.5 Math 1.5B | 数据集：MATH-format（train 7500 / val 5000）

---

## 1. Baseline 评估

直接用 vLLM 跑原始模型，**72.4% 的输出根本没有遵循格式**（type3），数学准确率只有 4.10%。这说明预训练模型不具备 `<think>...</think><answer>...</answer>` 格式能力，必须训练。

| accuracy | format_reward | type1 | type2 | type3 |
|----------|--------------|-------|-------|-------|
| 4.10% | 27.6% | 42/1024 | 241/1024 | 741/1024 |

---

## 2. SFT 训练

### 数据量 scaling

在不同训练集大小下跑 3 epochs，accuracy 单调递增。N=512 已达作业 15% 目标，说明数据质量比数量更重要（early gain is steep）。

| N | accuracy |
|---|---------|
| 128 | 11.0% |
| 256 | 12.5% |
| 512 | **15.2%** ✅ |
| 1024 | 16.4% |
| 7500 | **18.9%** |

### Filtered SFT（RFT）

用当时的 SFT 模型对全部 7500 题各采样 K=4 条，过滤出答案正确的约 1200 条，再做 SFT。结果和 N=1024 标准数据持平（16.0% vs 16.4%），验证了模型自生成的正确解可以替代人工标注数据。

---

## 3. Expert Iteration 训练

EI 的核心超参是 G（每题生成几条 rollout）、D_b（replay buffer 大小）、E（每步 SFT epochs）。

| Run | G | D_b | E | step5 acc |
|-----|---|-----|---|----------|
| G4_Db512_E1 | 4 | 512 | 1 | 13.9% |
| G4_Db1024_E1 | 4 | 1024 | 1 | 19.5% |
| G4_Db2048_E1 | 4 | 2048 | 1 | 18.8% |
| G8_Db1024_E1 | 8 | 1024 | 1 | 21.1% |
| G4_Db1024_E2 | 4 | 1024 | 2 | 18.5% |
| **G8_Db1024_E2** | **8** | **1024** | **2** | **21.9%** ★ |

**关键发现**：
- D_b=512 太小（buffer 容量不足，每步 SFT 样本重复率高），提到 1024 有显著收益（+5.6pp）；继续增大到 2048 没有帮助。
- G=8 比 G=4 更好，原因是更多 rollout 提高 rejection sampling 的命中率，让 buffer 里的正确解更多样化。
- E=2 只在 G 够大时有效：G=4 时 E=2 反而轻微过拟合（18.5% < 19.5%），G=8 时 E=2 还有正收益。

---

## 4. GRPO 训练

### 4.1 初始运行与 LR 扫参

从 `reinforce_with_baseline, lr=1e-5` 出发，256 步后卡在 20~23% 平台。扫了 5 个 lr：

| lr | peak | 问题 |
|----|------|------|
| 3e-6 | 14.5% | 欠拟合，loss 几乎不下降 |
| 1e-5 | 23.2% | 稳定但天花板低 |
| **1.5e-5** | **29.4%** | 最优，全程 entropy 健康 |
| 2e-5 | 26.8% | 可用 |
| 3e-5 | 36.0% | step 140 entropy collapse ⚠️ |

lr=3e-5 时模型出现 entropy collapse：entropy 快速降至接近 0，policy 退化为确定性输出，之后 accuracy 一路下滑无法恢复。

### 4.2 Ablation：去掉 baseline 会怎样（run6）

把 loss 从 `reinforce_with_baseline` 换成 `no_baseline`（不减组内均值）。格式能力反而更好（format_reward 95.1%），但数学准确率天花板更低（peak 24%）。

**原因**：没有 baseline，所有正向 reward 的样本梯度方向相同，无法区分"这道题做对了但其他同组做错的"；信号质量差，policy 退化成只会输出格式模板。

### 4.3 关键突破：masked_normalize（run7）

发现 loss 聚合方式影响很大：

- `masked_mean`：每个 token 的 loss 除以**该 response 的 token 数**（短短答案和长推理链平等对待）
- `masked_normalize`：统一除以 `max_gen_len`（长的正确推理链贡献更多梯度）

| loss_aggregation | use_std | peak |
|-----------------|---------|------|
| masked_mean | True | 29.4% |
| **masked_normalize** | **True** | **39.8%** ★ |
| masked_normalize | False | 31.5% |

`masked_normalize` 使 peak 从 29.4% → 39.8%（+10.4pp），这是整个 GRPO 阶段最大的单次提升。

### 4.4 Off-policy 实验：数值爆炸问题（run9A / run9B）

在 run7 基础上，把每个 rollout batch 复用多次（off-policy）：

**run9A（2 epochs）**：训练到 step 248 时 `grad_norm = 1.1 × 10¹³`，entropy 崩至 0.04，训练实际已失败。

**run9B（3 epochs）**：40 步内完全崩溃，accuracy 从 22.7% 直接跌到 2.1%，输出格式也全丢了。

**根因**：off-policy 时 policy 和 rollout 时的 old policy 之间的 log ratio 没有任何上界约束，`torch.exp(unbounded_value)` 直接数值爆炸。

**修复（P0）**：在 `compute_grpo_clip_loss` 中加一行：
```python
log_ratio = (policy_log_probs - old_log_probs).clamp(-20, 20)
```

### 4.5 更深的问题：run7 final checkpoint 实际已崩溃

run7 训练完看 1024 子集的 final accuracy 是 36.3%，以为没问题。但对全量 5000 条评估时，**accuracy = 0%**。

原因：run7 的 final checkpoint（step 256）发生了灾难性 entropy collapse，模型对所有输入只输出重复的 😉 emoji。1024 子集的 36.3% 是 checkpoint 还未完全崩溃时的一个历史快照——训练末期的崩溃是渐进的，而子集采样恰好错过了崩溃区域。

**这说明仅靠 1024 子集的 periodic eval 不足以检测末期崩溃**，需要在全量 val 上验证。

### 4.6 真正的修复：KL-in-loss（run10）

加入 KL 惩罚项，防止 policy 偏离初始 ref model 太远：

1. **log_ratio clamp（防数值爆炸）**
2. **KL-in-loss**：加载一份冻结的 ref model，用 k3 estimator 计算 per-token KL：
   ```python
   log_diff = (ref_log_probs - policy_log_probs).clamp(-20, 20)
   kl = torch.exp(log_diff) - log_diff - 1  # >= 0
   loss += kl_coef * masked_mean(kl, response_mask)
   ```
3. **group filter**：跳过组内 reward std ≈ 0 的 group（全对或全错，梯度为零，浪费算力）

扫 kl_coef（均为 2 epochs off-policy）：

| run | kl_coef | peak (1024) | final (5000) |
|-----|---------|------------|-------------|
| run7（无KL，1 epoch）| — | 39.8% | **0%** ❌ |
| **run10** | **3e-4** | **40.1%** | **46.05%** ✅ |
| run11 | 1e-3 | 23.2%（OOM终止）| — |
| run12 | 3e-3 | 24.0%（早停）| — |

run11 OOM：kl_coef=1e-3 时 KL loss 项的反向传播占用了更多激活内存，在 step 113 出现 OOM。说明 KL 惩罚过强不仅伤性能也会影响稳定性。

**run10 最终结果**：全量验证集 accuracy = **46.05%**（2302 / 4999），grad_norm 稳定在 0.1，entropy 全程维持在 0.32。

### 4.7 完整迭代路径

| run | 关键改动 | peak (1024) | final (5000) |
|-----|---------|------------|-------------|
| run1 | 初始 reinforce, lr=1e-5 | 23.2% | — |
| run5 | lr=1.5e-5 | 29.4% | — |
| run6 | no_baseline | 24.0% | — |
| run7 | +masked_normalize | 39.8% | **0%** ❌ |
| run9A | +2 epochs off-policy | 37.6% | — |
| **run10** | **+clamp+KL(3e-4)+group_filter** | **40.1%** | **46.05%** ✅ |

**最优配置（run10）**：
```
loss_type=grpo_clip, lr=1.5e-5, epochs=2
loss_aggregation=masked_normalize, use_std=True
kl_coef=3e-4, group_filter=True
log_ratio.clamp(-20, 20)
```
