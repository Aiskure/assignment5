# CS336 Assignment 5 实验记录

> 本文件供下一个 Claude 会话直接上手使用。读完本文件 + CLAUDE.md 即可继续工作。

---

## 项目整体状态

### 已完成（主作业）

主作业（SFT / Expert Iteration / GRPO）已全部完成，最终结果：

| 方法 | 数据集 | 最优 accuracy |
|------|--------|--------------|
| Baseline (Qwen2.5-Math-1.5B) | MATH validation 1024 | 4.1% |
| SFT full | MATH validation 1024 | 18.9% |
| Expert Iteration (G=8, Db=1024, E=2) | MATH validation 1024 | 21.9% |
| GRPO (run10, ep=2, kl=3e-4) | MATH validation 5000 | **46.05%** |

主作业输出全部在 `outputs/a5_main/`（grpo/ ei/ baseline/ sft/）。

---

## 当前任务：补充作业（Supplement）

补充作业当前已跑通 **Llama-3.1-8B base model** 上的 RLHF 主流程：Baseline 评估 → SFT → DPO。核心训练与主要评测已完成，当前剩余工作以文档整理、答题汇总与最终表述收尾为主。

### 模型

| 模型 | 路径 | 用途 |
|------|------|------|
| `Llama-3.1-8B` | `models/Llama-3.1-8B` | 被训练主角（base model，非 Instruct） |
| `Llama-3.3-70B-Instruct` | `models/Llama-3.3-70B-Instruct` | AlpacaEval / SimpleSafety 评判者 |

### 数据集

| 数据集 | 路径 | 用途 |
|--------|------|------|
| MMLU test | `data/MMLU/mmlu_test.jsonl` (14042条) | Phase 1 知识评估 |
| GSM8K test | `data/gsm8k/test.jsonl` (1319条) | Phase 1 数学评估 |
| AlpacaEval | `data/AlpacaEval/alpaca_eval.jsonl` (805条) | Phase 1/2 聊天能力评估 |
| SimpleSafetyTests | `data/SimpleSafetyTests/simple_safety_tests.jsonl` (100条) | Phase 1/2 安全性评估 |
| UltraChat-200K | `data/safety_augmented_ultrachat_200k_single_turn/` | Phase 2 SFT 训练数据 |
| HH-RLHF | `data/hh/` | Phase 3 DPO 训练数据 |

### 关键 Prompting 规范（所有任务通用）

- **System prompt**：所有任务统一用 `cs336_alignment/prompts/zero_shot_system_prompt.prompt` 包装 instruction
- **Stop sequence**：`["# Query:"]`（模型生成到下一轮对话开头即停）
- 格式：system prompt 包含 `# Query:\n\`\`\`{instruction}\`\`\`\n\n# Answer:\n\`\`\`` ，stop at `# Query:`

---

## Phase 1：零样本基线评估

### 已完成

#### Parser 实现（两个已通过 pytest）

- `cs336_alignment/utils.py::parse_mmlu_response(mmlu_example, model_output)`
  - 用 `\b([ABCD])\b` 正则找第一个独立字母，找不到返回 None
  - adapter 接线：`tests/adapters.py::run_parse_mmlu_response` ✓

- `cs336_alignment/utils.py::parse_gsm8k_response(model_output)`
  - 取输出中最后一个数字（`re.findall(r'-?\d[\d,\.]*', ...)`，取最后一项，去逗号）
  - adapter 接线：`tests/adapters.py::run_parse_gsm8k_response` ✓

#### 评估脚本（均已修正 system prompt + stop sequence）

- `cs336_alignment/mmlu_eval.py`
  - prompt：mmlu.prompt + zero_shot_system_prompt.prompt，stop=["# Query:"]
  - PBS：`run_mmlu_eval.pbs` → `outputs/a5_supplement/mmlu/mmlu_results.json`
  - **状态：已完成**

- `cs336_alignment/gsm8k_eval.py`
  - prompt：`{question}\nAnswer:` 经 zero_shot_system_prompt 包装，stop=["# Query:"]
  - `init_vllm` 增加 `max_model_len=4096` 参数（防止 Llama-3.1 默认 131072 OOM）
  - PBS：`run_gsm8k_eval.pbs` → `outputs/a5_supplement/gsm8k/gsm8k_results.json`
  - **状态：已完成**

- `cs336_alignment/alpaca_eval_baseline.py`
  - Step 1：8B 生成 → `outputs/a5_supplement/alpaca_eval/model_outputs.json`（JSON array 格式）
  - Step 2：调用 `alpaca_eval evaluate` CLI，使用 `scripts/alpaca_eval_vllm_llama3_3_70b_fn` 配置
  - annotator 模型路径已改为本地 `models/Llama-3.3-70B-Instruct`，tp=4
  - PBS：`run_alpaca_eval.pbs`（需 4 GPU）→ `outputs/a5_supplement/alpaca_eval/`
  - **状态：已完成**

- `cs336_alignment/sst_baseline.py`
  - Step 1：8B 生成 → `outputs/a5_supplement/safety/sst_model_outputs.jsonl`
  - Step 2：调用 `scripts/evaluate_safety.py`（官方提供，70B 判断 safe/unsafe）
  - PBS：`run_sst_eval.pbs`（需 4 GPU）→ `outputs/a5_supplement/safety/`
  - **状态：已完成**

### 已知问题 & 修复记录

| 问题 | 原因 | 修复 |
|------|------|------|
| GSM8K/MMLU 输出全是重复 token（`#Region#Region...`） | 缺少 system prompt + stop sequence，base model 做 text completion 陷入循环 | 加 zero_shot_system_prompt 包装 + stop=["# Query:"] |
| vLLM OOM：`max seq len (131072) > KV cache (41216)` | Llama-3.1-8B 默认 context 128K，1 卡放不下 | `init_vllm` 加 `max_model_len=4096` 参数 |
| Llama-3.1-8B 第 4 权重文件缺失 | 下载不完整 | 重新补下，1.17G 为正常大小（官方一致） |

---

## Phase 2：SFT（已完成）

- 训练方式：QLoRA（r=64, alpha=128）on Llama-3.1-8B
- 训练数据：`data/safety_augmented_ultrachat_200k_single_turn/`
- Adapter 路径：`outputs/sft_8b/run1_qlora_b16/adapter/`
- Merged bf16 模型：`outputs/sft_8b/run1_qlora_b16/merged_bf16/`（base + LoRA merge）
- 量化 checkpoint：`outputs/sft_8b/run1_qlora_b16/final_checkpoint/`（bitsandbytes 4-bit，vLLM 无法直接加载）

---

## Phase 3：DPO（已完成）

- 训练数据：Anthropic HH-RLHF（`data/hh/`，4 个 gzip jsonl）
- 双卡架构：π_θ on cuda:0（可训练），π_ref on cuda:1（冻结）
- 训练 756 步，val_accuracy: 0.00 → **0.34**
- 模型路径：`outputs/dpo/run1/final_model/`（bitsandbytes 4-bit，需 dequantize 后才能用 vLLM）
- Dequantized 模型：`outputs/dpo/run1/merged_fp32/`（可直接用于 vLLM 评测）

---

## 输出目录结构

```
outputs/
├── a5_main/               # 主作业结果（已完成）
│   ├── grpo/              # run1~run12
│   ├── ei/                # G4/G8 × Db512/1024/2048 × E1/E2
│   ├── baseline/          # Qwen2.5-Math baseline
│   └── sft/               # SFT N=128/256/512/1024/full + filtered
└── a5_supplement/         # 补充作业结果
    ├── mmlu/              # mmlu_results.json
    ├── gsm8k/             # gsm8k_results.json
    ├── alpaca_eval/       # model_outputs.json + leaderboard.csv + annotations.json
    └── safety/            # sst_model_outputs.jsonl + sst_annotated.jsonl
```

---

## 关键文件清单

| 文件 | 说明 |
|------|------|
| `cs336_alignment/utils.py` | parse_mmlu_response, parse_gsm8k_response, init_vllm（已加 max_model_len 参数） |
| `tests/adapters.py` | 已接：MMLU/GSM8K parser；未接：packed_sft, iterate_batches, dpo_loss |
| `cs336_alignment/mmlu_eval.py` | MMLU 评估脚本（system prompt + stop sequence ✓） |
| `cs336_alignment/gsm8k_eval.py` | GSM8K 评估脚本（system prompt + stop sequence ✓） |
| `cs336_alignment/alpaca_eval_baseline.py` | AlpacaEval 评估脚本（8B 生成 + 70B judge） |
| `cs336_alignment/sst_baseline.py` | SimpleSafetyTests 评估脚本（8B 生成 + 70B judge） |
| `cs336_alignment/prompts/zero_shot_system_prompt.prompt` | 通用 zero-shot system prompt（所有任务共用） |
| `scripts/alpaca_eval_vllm_llama3_3_70b_fn/configs.yaml` | AlpacaEval annotator 配置（已改为本地 70B 路径，tp=4） |
| `scripts/evaluate_safety.py` | 官方 SST 安全评估脚本（直接调用） |
| `run_mmlu_eval.pbs` | 1 GPU，MMLU |
| `run_gsm8k_eval.pbs` | 1 GPU，GSM8K |
| `run_alpaca_eval.pbs` | 4 GPU，AlpacaEval（8B 生成 + 70B judge） |
| `run_sst_eval.pbs` | 4 GPU，SST（8B 生成 + 70B judge） |

---

## 下一步优先级

1. **整理 supplement 的最终答题与文档表述**（特别是 AlpacaEval、SST、alignment tax 的解释）
2. **同步简历与面试材料**，明确区分主项目与补充项目
3. **如需继续补作业细节**，再决定是否补 `adapters.py` 中剩余 optional 部分

---

## Day 13 (2026-03-13)

### Phase 1 基线评估收尾

**MMLU 结果**（job 13153464，6min 33sec）：
- 总样本：14,042，准确率：0.0%，解析失败：14,042/14,042
- 吞吐量：~35.7 examples/sec
- 退化模式：`سوب` + `services` token 循环，符合预期（老师专设 parse failure 问题）

**GSM8K 结果**（job 13153559，2min 12sec）：
- 总样本：1,319，准确率：0.0%，解析失败：1,319/1,319
- 吞吐量：9.92 examples/sec（低于 MMLU，因 max_tokens=512 跑满）
- 退化模式：`سوب` + `Tall` + `services` token 循环

**AlpacaEval（job 13153533）**：
- Step 1（8B 生成）✅ 805 条输出已保存，Step 2（70B 打分）❌ HuggingFace 网络不通
- 修复：`--reference_outputs data/AlpacaEval/alpaca_eval.jsonl`，已重新提交

**SST（job 13153534）**：
- Step 1（8B 生成）✅ 100 条输出已保存，Step 2（70B 打分）❌ UUID CUDA_VISIBLE_DEVICES 崩溃
- 修复：Step 2 前加 UUID→整数转换，已重新提交

**答题记录**：`md/baseline_response.md`（MMLU + GSM8K 的 c/d/e/f 已完成，AlpacaEval/SST 待补）

### Phase 2 SFT 开始

**数据**：`data/safety_augmented_ultrachat_200k_single_turn/`（train + test，gzip jsonl）

**待实现（adapters.py）**：
1. `get_packed_sft_dataset` — PyTorch Dataset，token packing
2. `run_iterate_batches` — batch 迭代器
3. `run_compute_per_instance_dpo_loss` — DPO 损失（Phase 3）

**参考**：`assignment5_supplement_guide.md` 第二部分

---

## Day 14 (2026-03-16)

### DPO 训练完成

- Job `13185238`，756 步（~2 epochs），walltime ~10h
- val_accuracy 变化：0.00 → 0.295 → 0.325 → 0.335 → **0.340**（final）
- 最佳模型：step 756（acc=0.34），保存在 `outputs/dpo/run1/final_model/`

### SFT 模型 merge

- SFT checkpoint 是 bitsandbytes 4-bit 量化格式，vLLM 无法加载（`KeyError: layers.25.mlp.down_proj.weight.absmax`）
- 解决：用 `cs336_alignment/merge_qlora.py` 从 base Llama-3.1-8B (bf16) + LoRA adapter merge 出 full-precision 模型
- 输出：`outputs/sft_8b/run1_qlora_b16/merged_bf16/`

### SFT 后评测结果

| 评测 | Baseline (zero_shot) | SFT (alpaca) |
|------|---------------------|-------------|
| **MMLU** | 0.00% (14042), 34.82 ex/s | **22.82%** (3205/14042), 98.96 ex/s |
| **GSM8K** | 0.00% (1319), 9.86 ex/s | **1.90%** (25/1319), 72.11 ex/s |

- Baseline 0% 是因为 base Llama-3.1-8B 对 zero_shot_system_prompt 格式不理解，输出阿拉伯语乱码（`سوب`/`services` token 循环）
- MMLU 22.82%：SFT 让模型学会遵循 prompt 格式输出 A/B/C/D
- GSM8K 1.90%：Alpaca SFT 主要训练 instruction following，对数学推理帮助有限；62 个 parse failure
- AlpacaEval + SST 评测已提交（job 13189851, 13189852），等结果

### SFT 后评测完结（AlpacaEval + SST）

**AlpacaEval SFT**（job 13197136，gdev，~2h）：
- Win Rate: **1.93%**（15/805 wins），LC Win Rate: **1.28%**，平均回复长度 1845 tokens
- 绝对数值极低，参照系是 GPT-4 Turbo；SFT on Alpaca 对 instruction-following 质量有限提升

**SST SFT**（job 13189852，gdev）：
- Overall safe rate: **36%**（36/100）
- 按类别：Child Abuse 5%、Suicide/Self-Harm 35%、Scams/Fraud 40%、Illegal items 45%、Physical Harm 55%
- 严重失败：Child Abuse 类别模型**主动生成有害内容**（CSAM 相关），几乎从不拒绝；说明 Alpaca SFT 完全没有建立安全护栏

### DPO 模型 dequantize + 评测

- DPO 模型为 bitsandbytes 4-bit 格式（直接训练于量化 checkpoint，无 LoRA adapter）
- 用 `model.dequantize()` 转 fp32，保存至 `outputs/dpo/run1/merged_fp32/`（~15GB）
- vLLM 推理时自动使用 bf16，与名称 fp32 无精度差别

**DPO 后评测汇总**：

| 评测 | SFT | DPO | 变化 |
|------|-----|-----|------|
| MMLU | 22.82% | 22.18% | −0.64% |
| GSM8K | 1.90% | 2.12% | +0.22% |
| AlpacaEval (win rate) | 1.93% | 1.55% | −0.38% |
| AlpacaEval (LC win rate) | 1.28% | 0.92% | −0.36% |
| SST safe rate | 36% | 42% | +6% |

- **Alignment tax**：MMLU/GSM8K 变化极小，基本无退化
- **AlpacaEval**：DPO 略有下降，且 DPO 输出更长（2397 vs 1845 tokens）但质量未提升，LC win rate 更低
- **SST**：DPO 在 Child Abuse（+20%）和 Illegal items（+15%）上改善明显，但仍会生成有害内容，安全对齐不充分
