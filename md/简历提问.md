# 简历面试问题清单（无死角版）

---

## 一、教育背景

1. AI for Science 这个项目具体学了哪些课程？哪些跟你做的 post-training 方向直接相关？
2. 你本科是大气科学，为什么转到 AI 方向？大气科学的背景对你现在做 AI 有什么帮助吗？
3. 硕士期间有没有发表过论文？如果没有，为什么？
4. 你在 NUS 的 GPA 是多少？本科呢？

---

## 二、DPO 项目（基于 DPO 的大模型指令对齐与安全性评估）

### 2.1 项目整体理解

5. 用一两句话概括这个项目的核心贡献是什么？跟直接用开源 RLHF pipeline 比，你做了哪些独特的东西？
6. 这个项目是课程项目、实习项目还是个人项目？团队几个人？你具体负责什么？
7. 为什么选 Llama-3.1-8B 作为 base model？考虑过其他模型吗？

### 2.2 SFT 阶段细节

8. 为什么选 QLoRA 而不是 full fine-tuning？你能推导一下 LoRA 的参数量计算吗？假设 hidden_dim=4096, rank=16, 有多少个 adapter 参数？
9. QLoRA 用的是什么量化方案？NF4 是什么？跟 INT4 有什么区别？NF4 的核心思想是什么？
10. QLoRA 中 double quantization 是什么意思？为什么要做？
11. UltraChat-200K 和 SafetyTunedLlamas 这两个数据集各有多少条数据？你怎么混合的？混合比例是多少？为什么是这个比例？
12. SFT 的训练超参数是什么？learning rate, batch size, epochs, warmup steps, scheduler 分别是什么？
13. SFT 用了多少张卡？训练了多久？显存占用是多少？
14. SFT 阶段 loss 曲线是什么样的？有没有过拟合的迹象？你怎么判断的？
15. 为什么 SFT 前 MMLU 是 0%？Llama-3.1-8B 本身不是有 instruction 能力的吗？你用的是 base model 还是 instruct model？
16. "推理吞吐量提升 2.8×" 是怎么算的？跟谁比？为什么 SFT 之后吞吐量会提升？

### 2.3 DPO 阶段细节

17. **请你从 RLHF 的目标函数出发，推导 DPO 的 loss function。** 每一步的数学变换要写清楚。
18. DPO 的核心假设是什么？什么情况下这个假设会不成立？
19. DPO 相比 PPO 的优势和劣势各是什么？在什么场景下 PPO 会比 DPO 更好？
20. Anthropic HH-RLHF 数据集的数据量大约多少？chosen 和 rejected 分别是怎么构建的？
21. "双卡部署架构将可训练策略 πθ 与冻结参考模型 πref 分卡放置以规避显存竞争"——具体怎么实现的？用的是 `device_map` 还是手动 `.to(device)` ？参考模型需要梯度吗？为什么要冻结？
22. 如果只有一张卡，你有什么替代方案来放下 policy 和 reference model？（提示：TRL 的做法是什么？）
23. DPO 的 β 参数你设的多少？β 的物理含义是什么？β 太大和太小分别会怎样？
24. DPO 训练的学习率是多少？跟 SFT 阶段比有什么不同？为什么？
25. DPO 训练过程中你监控了哪些指标？rewards/chosen 和 rewards/rejected 的 margin 变化趋势是什么样的？
26. 有没有出现 reward hacking 的现象？如果有，你怎么处理？

### 2.4 安全性评估与定向补强

27. SST 是什么评测？全称是什么？它评测的是什么维度的安全性？
28. SST 的 5 个类别分别是什么？各自的安全率在 DPO 前后是多少？
29. BeaverTails 数据集是什么？它的 14 类危害标注具体有哪些？你怎么映射到 SST 的 5 类的？映射逻辑是什么？
30. "均衡采样" 具体怎么做的？上采样还是下采样？每类多少条？
31. 定向 DPO 二次训练的时候，会不会导致之前 SFT/DPO 学到的指令遵循能力退化（catastrophic forgetting）？你怎么验证的？
32. 安全率从 42% → 71%，那剩下的 29% 失败案例是什么样的？你分析过吗？

### 2.5 评测流水线

33. "LLM-as-Judge 使用 70B 模型"——具体用的是哪个 70B 模型？为什么用 70B 而不是 GPT-4？
34. AlpacaEval 的 LC win rate 只有 1.28%，这个数字说明什么？你觉得低吗？跟同类工作比怎么样？
35. "MMLU/GSM8K 对齐税可忽略（−0.64% / +0.22%）" 这个结论是怎么得出的？有做显著性检验吗？
36. 四维评测流水线你怎么保证可复现性？用了固定 seed 吗？推理温度是多少？
37. MMLU 评测是 0-shot 还是 5-shot？用的是什么 prompt 模板？
38. GSM8K 评测的答案提取逻辑是怎样的？如果模型输出格式不符合预期怎么处理？

---

## 三、GRPO 项目（基于 GRPO 的数学推理模型对齐训练系统）

### 3.1 项目整体

39. GRPO 跟 PPO 的核心区别是什么？GRPO 为什么不需要 Critic（Value Network）？
40. Group-relative advantage 的计算公式是什么？请写出来。
41. 这个项目是 CS336 的作业还是你自己额外做的？如果是作业，哪些部分是你自己扩展的？
42. 为什么选 Qwen2.5-Math-1.5B？为什么不用更大的模型？

### 3.2 数据与评测

43. MATH 数据集一共多少道题？你怎么划分训练集和测试集的？"seeded random 评测抽样" 具体怎么做的？
44. "基于正则表达式的数学答案评分器"——你的正则表达式能处理哪些情况？比如 $\frac{1}{2}$ 和 $0.5$ 算等价吗？$\sqrt{2}$ 和 $1.414$ 呢？
45. `<think>/<answer>` 格式是谁定义的？如果模型输出不包含这个标签，你怎么处理？这会不会导致评分偏差？
46. 评测用的 vLLM 推理参数是什么？temperature, top_p, max_tokens 分别是多少？

### 3.3 SFT 基线

47. "5 个数据规模下验证 data scaling law"——5 个数据规模分别是多少？scaling curve 是什么形状的？是 log-linear 的吗？
48. "微批次训练" 是什么意思？跟 gradient accumulation 是一回事吗？为什么需要微批次？
49. SFT 的 4.1% → 18.9%，你觉得这个提升主要来自哪里？是学到了数学知识还是学到了输出格式？你怎么区分？

### 3.4 Expert Iteration

50. Expert Iteration 的具体流程是什么？用自己的话描述一遍。
51. Rejection sampling 的筛选标准是什么？只保留答案正确的样本吗？那岂不是在用 ground truth label 做训练？
52. "rollout 数 / 回放缓冲区大小 / SFT 轮数" 这 6 组实验具体的变量设置是什么？关键结论是什么？
53. EI 相比纯 SFT 只从 18.9% → 21.9%，提升不大。你分析过为什么吗？

### 3.5 GRPO 核心实现

54. **请你从零手写 GRPO 的训练循环伪代码**，包括 rollout → advantage 计算 → loss 计算 → 梯度更新。
55. PPO-clip loss 的公式是什么？clip 的 ε 你设的多少？为什么是这个值？
56. Group-relative advantage 归一化具体怎么做的？是减均值除标准差吗？如果一个 group 内所有样本都对/都错，advantage 会怎样？你怎么处理的？
57. 你简历提到做过 "masked_normalize ablation"——具体什么是 masked_normalize？你消融了什么？结论是什么？
58. "entropy collapse diagnosis via KL divergence penalty"——entropy collapse 的表现是什么？你怎么检测到的？KL penalty 加在 loss 的哪里？系数是多少？
59. "gradient explosion fix via log importance ratio clamping"——importance ratio 是什么？为什么取 log？clamping 的范围是什么？为什么会梯度爆炸？
60. On-policy 和 off-policy 的区别是什么？在 GRPO 里分别怎么实现的？off-policy 会引入什么问题？你怎么缓解的？
61. 12 组控制变量实验具体控制了哪些变量？最重要的 3 个 finding 是什么？
62. 梯度累积的步数是多少？为什么需要梯度累积？它跟 effective batch size 的关系是什么？

### 3.6 训练工程

63. 训练用了几张 GPU？什么型号？训练了多长时间？
64. rollout 阶段用 vLLM，训练阶段用 PyTorch，这两者之间模型权重怎么同步的？
65. W&B 上你具体追踪了哪些指标？能不能举一个你从 W&B 图表中发现 bug 或做出决策的例子？
66. 46.1% 的准确率在 MATH 上是什么水平？跟同参数量的 SOTA 比怎么样？

---

## 四、技术深挖（Transformer & LLM 基础）

### 4.1 模型架构

67. Transformer 的 self-attention 的计算公式是什么？时间复杂度和空间复杂度各是多少？
68. Multi-Head Attention 的参数量怎么算？如果 d_model=4096, n_heads=32，每个 head 的 d_k 是多少？
69. GQA（Grouped Query Attention）是什么？跟 MHA 和 MQA 有什么区别？Llama 系列用的是哪种？
70. RoPE 的核心思想是什么？它跟绝对位置编码和相对位置编码比，优势在哪？能不能写出 RoPE 的旋转矩阵公式？
71. Pre-Norm 和 Post-Norm 有什么区别？为什么现在主流模型都用 Pre-Norm？
72. RMSNorm 和 LayerNorm 有什么区别？RMSNorm 的公式是什么？为什么它更快？
73. SwiGLU 激活函数是什么？写出公式。它跟 ReLU/GELU 比优势在哪？
74. MoE（Mixture of Experts）的原理是什么？load balancing loss 是什么？为什么需要它？
75. KV Cache 是什么？它在推理时能节省多少计算？它的显存开销怎么估算？
76. FlashAttention 的核心思想是什么？它为什么能加速？是减少了计算量还是减少了显存访问？tiling 策略是怎么工作的？

### 4.2 训练技术

77. BF16 和 FP16 的区别是什么？各自的指数位和尾数位分别是多少？为什么训练更推荐 BF16？
78. Mixed precision training 的流程是什么？为什么需要 loss scaling？
79. Gradient checkpointing 是什么？它用时间换空间的 trade-off 大概是多少？
80. DeepSpeed ZeRO 有几个 stage？每个 stage 分别做了什么？你用过哪个 stage？
81. FSDP 和 DeepSpeed ZeRO-3 有什么区别？你在项目中用的是哪个？为什么？
82. DDP（DistributedDataParallel）的通信原理是什么？AllReduce 的过程是怎样的？

### 4.3 LoRA & 微调

83. LoRA 的数学原理是什么？为什么低秩近似是合理的？有没有理论依据？
84. LoRA 一般加在模型的哪些层？为什么？加在 attention 的 Q, K, V, O 上效果有区别吗？
85. LoRA 的 rank 和 alpha 的关系是什么？alpha/rank 这个 scaling factor 为什么重要？
86. QLoRA 的完整流程是什么？从量化 base model → 插入 adapter → 训练 → 合并，每一步的细节。
87. LoRA 合并（merge）的时候怎么做的？合并后的权重是什么？

### 4.4 RLHF / 偏好对齐

88. RLHF 的完整 pipeline 是什么？分几个阶段？每个阶段的输入输出是什么？
89. PPO 的目标函数是什么？clip 的作用是什么？GAE 是什么？λ 参数的作用是什么？
90. PPO 中 Actor 和 Critic 分别是什么？它们共享参数吗？
91. Reward model 的训练目标是什么？loss function 是什么？跟 DPO 的 loss 有什么关系？
92. DAPO 是什么？跟 GRPO 比有什么改进？
93. GSPO 和 REINFORCE 分别是什么？它们跟 GRPO 的关系是什么？
94. DPO 有哪些已知的问题/局限性？（比如 distribution shift, length bias 等）
95. IPO、KTO、ORPO 这些 DPO 变体你了解吗？它们各自解决了 DPO 的什么问题？

---

## 五、工程能力深挖

96. vLLM 的 PagedAttention 是什么？它怎么管理 KV Cache 的显存？
97. 你在 NSCC 上提交 PBS 任务的流程是什么？一个典型的 PBS 脚本长什么样？
98. 你用过 SLURM 吗？跟 PBS 有什么区别？
99. 训练过程中遇到过 OOM 吗？你怎么排查和解决的？
100. 训练过程中遇到过 NaN loss 吗？可能的原因有哪些？你怎么 debug 的？
101. HuggingFace Transformers 的 `Trainer` 和 TRL 的 `DPOTrainer` 你用了哪个？有没有自己写训练循环？
102. bitsandbytes 的 `Linear4bit` 底层是怎么实现的？推理时怎么做反量化？
103. flash-attn 你是怎么安装的？遇到过兼容性问题吗？它支持哪些 GPU 架构？
104. 多卡训练时你怎么设置 `CUDA_VISIBLE_DEVICES`？有没有遇到过 NCCL 通信错误？
105. Git 工作流你用的是什么？有没有 CI/CD 经验？

---

## 六、简历措辞推敲 & 潜在追问

106. "输出退化为无意义 token 循环"——具体是什么表现？能举个例子吗？你怎么诊断出这是 token 循环而不是其他问题？
107. "隐式奖励直接优化人类偏好，无需显式奖励模型"——请解释"隐式奖励"的数学含义。DPO 的 implicit reward 公式是什么？
108. "通过隐式奖励直接优化人类偏好"——这句话是 DPO 论文的标准描述。请问你是否真的理解，还是在复述论文？如果让你不用这句话，你怎么向一个不懂 ML 的人解释 DPO？
109. "三条路线从同一 base model 出发"——这三条路线是完全独立的实验吗？还是 EI 是建立在 SFT 之上，GRPO 是建立在 SFT 之上？
110. 你写了"善于利用 AI 辅助开发工具（Claude Code、Codex）加速工程迭代"，那请问在你的项目中，哪些部分是你自己写的，哪些是 AI 辅助生成的？你怎么保证 AI 生成的代码是正确的？
111. 简历里没有提到任何实习经历，为什么？你有过工业界的经验吗？

---

## 七、算法 & Coding 现场考察

112. 手写一个 Multi-Head Attention 的 forward 函数（PyTorch），不许用 `nn.MultiheadAttention`。
113. 手写 RoPE 的实现，给定位置 pos 和 dim，返回旋转后的 Q 和 K。
114. 手写 DPO loss 的计算代码，输入是 policy_logps_chosen, policy_logps_rejected, ref_logps_chosen, ref_logps_rejected 和 beta。
115. 手写 GRPO 的 advantage 计算和 PPO-clip loss。
116. LeetCode 200: Number of Islands —— 请用 BFS 和 DFS 两种方法实现。
117. LeetCode 215: Kth Largest Element in an Array —— 请用快速选择算法实现，分析平均和最坏时间复杂度。
118. 实现一个 LRU Cache（LeetCode 146），要求 O(1) 的 get 和 put。

---

## 八、系统设计 & 开放问题

119. 如果让你从头设计一个 RLHF 训练系统，你会怎么设计？考虑模型并行、数据并行、rollout 效率、显存管理。
120. 如果你的 DPO 训练后，模型在 AlpacaEval 上 win rate 很低，但安全性很高，你怎么分析原因并改进？
121. 假设你现在有 8 张 A100-80G，要训练一个 70B 的模型做 DPO，你会怎么分配资源？需要什么并行策略？
122. 你怎么看 DPO vs RLHF(PPO) 的未来发展？哪个更有前途？为什么？
123. GRPO 在数学推理之外的任务上效果怎么样？有什么局限性？
124. 如果给你一个新的 base model 和一个新的任务（比如代码生成），你会怎么设计 post-training 的完整流程？
125. Scaling law 在 post-training 阶段适用吗？你怎么理解 post-training 的 scaling？

---

## 九、行为面试 & Soft Skills

126. 描述一个你在项目中遇到的最大的技术困难，你是怎么解决的？
127. 你怎么决定一个实验的优先级？当你有 10 个想法但只有时间做 3 个，你怎么选？
128. 你在团队合作中通常是什么角色？有没有跟别人发生分歧的经历？
129. 你为什么想做 post-training / alignment 方向？你对这个方向未来 3-5 年的判断是什么？
130. 你的短期和长期职业目标是什么？